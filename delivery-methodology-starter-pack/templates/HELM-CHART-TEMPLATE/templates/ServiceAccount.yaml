apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "myapp.fullname" . }}
  labels:
    {{- include "myapp.labels" . | nindent 4 }}
  # No annotations here — workload identity bindings (Azure Workload Identity,
  # AWS IRSA, GKE Workload Identity, etc.) are configured by the DevOps team.
