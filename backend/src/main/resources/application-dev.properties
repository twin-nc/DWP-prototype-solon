# Dev (AKS) overrides — values injected by Kubernetes secrets/ConfigMap
# DATABASE_URL, DB_USERNAME, DB_PASSWORD, KEYCLOAK_ISSUER_URI all come from env vars
logging.level.root=info
logging.level.com.netcompany.dcms=info
